# Appwrite Assistant UI

Vite + React + TypeScript chat UI (shadcn). Served by nginx; `/api` proxies to the assistant engine.

```bash
npm ci
npm run dev
```

Production image is built via the root `docker compose` `ui` service.
